<?xml version="1.0" encoding="UTF-8"?>
<processDefinition name="OrderProcess">
    <activities>
        <jdbcActivity name="SelectCustomer">
            <username>admin</username> <!-- ❌ Hardcoded username -->
            <password></password>      <!-- ❌ Empty password -->
            <timeout></timeout>        <!-- ❌ Missing timeout value -->
        </jdbcActivity>
        <jdbcActivity name="UpdateOrder">
            <username>${DB_USER}</username>  <!-- ✅ Correct usage -->
            <password>${DB_PASSWORD}</password> <!-- ✅ Correct usage -->
            <timeout>30000</timeout>          <!-- ❌ Missing timeout end tag -->
        </jdbcActivity>
    </activities>
</processDefinition>
