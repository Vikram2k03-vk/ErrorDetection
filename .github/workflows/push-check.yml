name: XML Validation

on:
  pull_request:
    paths:
      - '**/*.xml'  

jobs:
  validate-xml:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Get changed XML files
        id: changed-files
        uses: tj-actions/changed-files@v44
        with:
          files: |
            **/*.xml

      - name: Validate changed XML files
        run: |
          set -e
          ERROR=0

          for file in ${{ steps.changed-files.outputs.all_changed_files }}; do
            echo "🔍 Checking $file"
            if ! xmllint --noout "$file"; then
              echo "❌ Invalid XML: $file"
              ERROR=1
            else
              echo "✅ Valid: $file"
            fi
          done

          if [ "$ERROR" -ne 0 ]; then
            echo "❌ XML validation failed."
            exit 1
          else
            echo "✅ All changed XML files passed validation."
          fi
